# Variables de entorno de ejemplo para VisionLap-RC
# Copia este archivo a .env y ajusta los valores según tu configuración.
# NO subas el archivo .env a tu repositorio de git.

# --- Base de Datos ---
# Define la URL de conexión a la base de datos.
# Para SQLite (por defecto): sqlite:///visionlap.db
# Para PostgreSQL: postgresql://user:password@host/dbname
# Para MySQL: mysql://user:password@host/dbname
DATABASE_URL=sqlite:///visionlap.db

# --- Seguridad ---
# Clave secreta para proteger las sesiones y los formularios contra CSRF.
# Debe ser un valor largo, aleatorio y difícil de adivinar.
# Puedes generar una con: python -c 'import secrets; print(secrets.token_hex(16))'
SECRET_KEY=pon_aqui_una_clave_secreta_y_larga

# --- Configuración de la Cámara (Básica) ---
# Índice de la cámara a utilizar. 0 suele ser la cámara web integrada.
CAMERA_IDX=0
# Resolución de la cámara (ancho y alto).
CAMERA_WIDTH=640
CAMERA_HEIGHT=480

# --- Configuración de la Cámara (Avanzada) ---
# NOTA: La disponibilidad y rango de estos parámetros dependen de la cámara y su driver.
# Un valor de -1 significa que la configuración no se aplicará.
# Experimenta para encontrar los mejores valores para tu entorno.

# Fotogramas Por Segundo (FPS) deseados.
CAMERA_FPS=60

# Autoenfoque: 1 para activar, 0 para desactivar (y usar enfoque manual).
CAMERA_AUTOFOCUS=-1
# Enfoque manual (ej. 0-255). Solo si autoenfoque está en 0.
CAMERA_FOCUS=-1

# Modo de exposición automática. Para OpenCV en Windows con cámaras UVC:
# 1 = manual, 3 = automático (aperture priority).
CAMERA_AUTO_EXPOSURE=-1
# Exposición manual (ej. -7 a -2, valores más bajos son más rápidos/oscuros).
# Solo si la exposición automática está en modo manual (1).
CAMERA_EXPOSURE=-1

# Ganancia de la cámara (ej. 0-255). Útil para condiciones de poca luz.
CAMERA_GAIN=-1
# Brillo (ej. 0-255).
CAMERA_BRIGHTNESS=-1
# Contraste (ej. 0-255).
CAMERA_CONTRAST=-1

# --- Configuración del Detector ---
# Puerto local para bloquear el acceso concurrente a la cámara.
DETECTOR_LOCK_PORT=57001

# --- Configuración de la Línea de Meta ---
# Coordenadas de los dos puntos que definen la línea: (X1, Y1) y (X2, Y2).
FINISH_LINE_X1=100
FINISH_LINE_Y1=240
FINISH_LINE_X2=540
FINISH_LINE_Y2=240